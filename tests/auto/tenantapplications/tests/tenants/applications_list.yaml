tests:
    tenants/applications_list_Get a list of referenced applications on a given tenant (from management tenant):
        command: c8y tenants applications list --tenant "mycompany"
        exit-code: 0
        stdout:
            json:
                method: GET
                path: /tenant/tenants/mycompany/applications
