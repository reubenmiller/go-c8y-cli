tests:
    deviceprofiles_list_Get a list of device profiles:
        command: c8y deviceprofiles list
        exit-code: 0
        stdout:
            json:
                method: GET
                path: /inventory/managedObjects?query=$filter=((type eq 'c8y_Profile') and (name eq '{name}')) $orderby=name asc
