tests:
    configuration_create_Create a configuration package:
        command: c8y configuration create --name "agent config" --description "Default agent configuration" --configurationType "agentConfig"
        exit-code: 0
        stdout:
            json:
                body.configurationType: agentConfig
                body.description: Default agent configuration
                body.name: agent config
                method: POST
                path: /inventory/managedObjects
